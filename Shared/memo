
TextField 为 编辑框
command + /  添加 删除 注释

mac和ios 界面 区分问题

直接进项目目录 可直接调用git 命令

xcode  使用问题：
     1. 无法双击选中中文字段
     2. 不能快速复制 （需设置）

swift 问题：



swiftUI 问题：
    1. 子view 事件 如何通知父view （swiftUI 2.0 onChange？）
    (只能在赋值时， 把相互关系处理。   )
    
    2. 全局变量 储存数据 试改
    3. State 了解
    
    4. onchange跟view对象无关，只跟传入变量有关。
    
       数据变化时， 异步调用onchange


编写问题遗留：

    1. 选定键盘 和 键盘退出问题。。
    2. 计算交互
    3. 灵活修改文字描述， 但不修改变量  (ok)
    
    
    4. (总还款额)精确尾值处理
    5. 根据输入实时变化 或 回车变化 (ok)
    
    6. 根据分期费率 反推 年利率
    
    7. 留下， fee，rate 没一起更新时的bug



方法备忘：
    1. for (index,value) in shoppingList.enumerate()
    
    2.  // Equivalent to movies.map { $0.name }
        let movieNames = movies.map(\.name)
